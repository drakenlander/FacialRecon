<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SignUpActivity">

    <EditText
        android:id="@+id/username_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="150dp"
        android:layout_marginEnd="32dp"
        android:hint="Nombre de Usuario"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:id="@+id/password_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="Contraseña"
        android:inputType="textPassword"
        app:layout_constraintEnd_toEndOf="@+id/username_input"
        app:layout_constraintStart_toStartOf="@+id/username_input"
        app:layout_constraintTop_toBottomOf="@+id/username_input" />

    <EditText
        android:id="@+id/confirm_password_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="Confirmar Contraseña"
        android:inputType="textPassword"
        app:layout_constraintEnd_toEndOf="@+id/password_input"
        app:layout_constraintStart_toStartOf="@+id/password_input"
        app:layout_constraintTop_toBottomOf="@+id/password_input" />

    <Spinner
        android:id="@+id/security_question_spinner"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintEnd_toEndOf="@+id/confirm_password_input"
        app:layout_constraintStart_toStartOf="@+id/confirm_password_input"
        app:layout_constraintTop_toBottomOf="@+id/confirm_password_input" />

    <EditText
        android:id="@+id/security_answer_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="Respuesta"
        android:inputType="text"
        app:layout_constraintEnd_toEndOf="@+id/security_question_spinner"
        app:layout_constraintStart_toStartOf="@+id/security_question_spinner"
        app:layout_constraintTop_toBottomOf="@+id/security_question_spinner" />

    <RadioGroup
        android:id="@+id/role_radio_group"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="@+id/security_answer_input"
        app:layout_constraintStart_toStartOf="@+id/security_answer_input"
        app:layout_constraintTop_toBottomOf="@+id/security_answer_input">

        <RadioButton
            android:id="@+id/role_1_radio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Operador" />

        <RadioButton
            android:id="@+id/role_2_radio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Administrador de Datos" />

    </RadioGroup>

    <Button
        android:id="@+id/sign_up_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="Registrar Nuevo Usuario"
        app:layout_constraintEnd_toEndOf="@+id/role_radio_group"
        app:layout_constraintStart_toStartOf="@+id/role_radio_group"
        app:layout_constraintTop_toBottomOf="@+id/role_radio_group" />

</androidx.constraintlayout.widget.ConstraintLayout>